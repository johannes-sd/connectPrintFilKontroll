    {{> head}}
<body>
    {{> toppmeny}}

        <div id="Velkommen">
            <table class="display">
                <thead>
                    <tr>
                        <th>
                            Connect-filer
                        </th>
                    </tr>
                </thead>
            </table>
        </div>

    {{> printModal}}


    </body>
    {{> printModalScript}}
    <script>
    
 
$(document).ready(function() {

    let tabell = $('.display').DataTable( {
        "ajax" : {
            "url": "/filliste",
            "contentType": "application/json",
            "type": "POST"
        },
        "columns" : [{"data" : null}],
        "select" : "os"
    });

    //Markere valgte elementer
    $('.display table').on( 'click', 'tr', function () {
        if ( $(this).hasClass('selected') ) {
            $(this).removeClass('selected');
            console.log($(this));
        }
        else {
            tabell.$('tr.selected').removeClass('selected');
            $(this).addClass('selected');
            console.log($(this));
        }
    });

    //Tester knapp for print.
    $( "#scanneliste" ).click(function() {
        alert( "Handler for .click() called.");
        var noedata = tabell.rows({selected: true}).data().toArray();  //Innebygd API for håndtering av rows.
        console.log(JSON.stringify(noedata, undefined, 2));
      });

    dialog.button().on( "click", function() {
      alert("Prindialogknapp knappet");
    });

});
    </script>
</html>